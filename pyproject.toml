[project]
name = "registry-sentinel"
version = "0.1.0"
description = "Audit-ready data platform ingesting UK Companies House data."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx",
    "pydantic",
    "pydantic-settings",
    "psycopg2-binary",
    "tenacity",
    "dbt-core>=1.11,<1.12",
    "dbt-postgres>=1.11,<1.12",
    "dagster",
    "dagster-webserver",
    "streamlit",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "respx",
    "ruff",
    "sqlfluff",
    "sqlfluff-templater-dbt",
    "pre-commit",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/registry_sentinel"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: requires a live Postgres instance (set TEST_DATABASE_URL)",
]
